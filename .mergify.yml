pull_request_rules:
- name: automatic squash-and-merge on CI success and review
  conditions:
  - "#approved-reviews-by>=1"
  actions:
    queue:
      name: default
      method: squash
      update_method: merge
